enum fliterOptions{
     MainCourse,Desserts,Lunch,SideDishes,Breakfast,Soup,SnacksandSandwiches;
  }

class FilterOption{
  String filterOptionName;
  bool isSelected;
  FilterOption({required this.filterOptionName,required this.isSelected});
}

